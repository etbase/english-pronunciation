# 此檔案為範例，說明未來各階段會用到哪些環境變數。
# 請複製一份為 .env（該檔案已被 .gitignore 排除，不會被提交），並填入實際值。
#
# 注意：目前（MVP 階段）專案是純靜態網站，沒有建置流程，
# 瀏覽器端 JS 尚無法直接讀取這裡的變數。
# 這些變數會在實際開發對應階段功能時，決定好載入方式後才會真正生效。

# ------------------------------
# 第一階段：Firebase（登入 / 資料儲存 / 錄音檔儲存）
# ------------------------------
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# ------------------------------
# Azure Speech（只給 Azure Function 後端使用）
# ------------------------------
# 真實 Key 不要寫在這個檔，也不要寫進前端任何 JS。
# 本機請複製 api/local.settings.json.example → api/local.settings.json 後自行填入。
# 正式環境請在 Azure Portal → Function App → Settings → Environment variables 設定：
#   AZURE_SPEECH_KEY=YOUR_KEY_HERE
#   AZURE_SPEECH_REGION=YOUR_REGION_HERE
#   ALLOWED_ORIGINS=https://USERNAME.github.io

# ------------------------------
# 第三階段：金流 / 會員付費（尚未開發，先預留）
# ------------------------------
# STRIPE_SECRET_KEY=
# STRIPE_PUBLISHABLE_KEY=
